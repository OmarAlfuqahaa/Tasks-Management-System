/**
 * Central place to keep the base URL used by all API calls.
 * Default leverages Angular's dev proxy (/proxy.conf.json) so we can
 * keep relative URLs and avoid hardcoding ports in multiple files.
 */
export const API_BASE_URL = 'http://localhost:5258/api';


